"""Shared risk engine (doc 00 §5). Pure sizing + stateful engine. In PAPER these
rules run in simulation so paper results are realistic; they are NOT live gates
(the only reason live is unavailable is the missing venue execution adapter)."""
