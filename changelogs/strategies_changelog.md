# strategies module changelog (backend/app/strategy_engine, routers/strategies.py, design-b Strategies screens)

## 2026-09-03/04 — NO GATES: all seven rows evaluating + paper-trading, pages rebuilt, backtests wired
**Why**: owner directive — "Remove every gate from the strategy engine and get all six strategies visibly evaluating and paper-trading NOW. Paper only." Evidence collection, not capital protection.
**Files**: `strategy_engine/{evaluator,scheduler,worker,mode,contract,seed}.py`, `strategy_engine/strategies/{base,common,cohort,s01..s06}.py`, `strategy_engine/execution/manager.py`, `strategy_engine/features/indicators.py`, `strategy_engine/backtest/runner.py`, `routers/strategies.py` (rewritten), `tests/strategy_engine/{test_no_gates,test_kill_switch,test_manager}.py`, `frontend/src/{types/strategies.ts,lib/strategiesApi.ts,design-b/screens/StrategiesOverviewB.tsx,design-b/screens/StrategyDetailB.tsx}`.
**What changed**
- No history gates: every condition evaluates over the available window; short history = `warming: M of N <unit>` on the condition and the signal; feeds that do not exist for a bar = `unavailable (not in score)`. Thresholds verbatim from the docs.
- No mode gates: `compute_effective_mode` = paper→paper, off→off, live→off (`LIVE_DISABLED_REASON="no execution adapter built"`, PATCH /mode returns 409 for live). Promotion/PF/data-age/custody gates removed.
- Risk engine annotates only in paper: size still 1.5% risk / 3x cap; daily cap, consecutive losses, PF<0.9, concurrency, duplicate-direction written on the signal as `risk engine would have: <rule>`; paper trade proceeds. Paper-paused states and `/rearm` removed.
- s03 cohort from today's metrics (`cohort: 7d metrics · fill history Nd of 90d (90d at ~2026-11-24)`), upgrades automatically. s01 cascade detector runs on partial liquidation coverage (`liq_coverage: partial` label).
- Backtests: worker runs `run_all` in the background at startup + every 24h → `backend/backtest/results/{sid}.md|.json`; API serves the cache (`GET /{sid}/backtest`, `refresh=true` replays inline); coverage table + `unavailable (not in score)` per condition in every report.
- API: `GET /signals/recent` (24h, every evaluation, consecutive identical skips collapsed with a count — number-masked reason compare), `GET /trades/open`, `/risk` shows paper equity $1,000 + real paper P/L, per-strategy `evals_24h/setups_24h/fires_24h`, `trades_30d/win_rate_30d/pf_30d/net_30d`, `warming`, `labels`, `last_eval_ts`; detail carries `latest[asset]` signal (conditions + warming + intents + fire_threshold), `stats{7d,30d,90d,all}`, backtest meta.
- Pages: overview (global mode Paper, Live disabled with reason, risk strip, "last evaluated Xs ago", real counts, open paper positions, Recent signals all/fired/skipped) and detail (live auto-refreshing evaluation log last 50, warming tags on conditions, Backtest drawer with cached meta + Re-run). All stale "off"/"Phase 1" copy removed.
- Prod hotfixes during verification (same day):
  1. `Object of type bool_ is not JSON serializable` on the strat_signals INSERT for the 15m strategies (numpy bools from s06 `pb_ok`, OI sign compare) lost the whole tick's commit → `_py()` sanitiser in `evaluator._log` + `Cond.as_dict` coerces `met` to bool; `_route` coerces px/stop/target/size to float. Tests: `test_signal_payload_is_plain_python_json`, `test_py_coerces_numpy_scalars`, `test_cond_as_dict_coerces_numpy_met`.
  2. Regime gate rendered `blocked: [, ", o, r, a, c, l, e …` — `strat_regime.blockers` (JSON column) arrives as a str through `text("SELECT *")`; `regime_blockers()` decodes it (base.regime_cond + s06 macro check). Test: `test_regime_blockers_json_string_is_decoded_not_iterated`.
  3. Worker stop hit systemd's 90s timeout (SIGKILL) because `asyncio.run()` joins the backtest replay thread at exit → `runner.STOP` threading.Event checked per bar (`ReplayAborted`), set in the worker's `finally`. Verified on the 19:48 UTC restart: stop 1.3s, "stopped cleanly".
  4. s05 "High-vol day: warming: 2 of 30 days of RV" despite 52 days of 15m candles — the live `_ctx` loader window (last 300 bars) capped doc-05's RV24h pct(30d) → new `StrategyContext.closes_15m_30d` (last 3000 15m closes) in `_ctx` and in the replay (`warm_from = start - 3000 bars`, `c15[i-2999:i+1]`); s05 uses it. Test: `test_s05_rv_percentile_uses_30d_closes_not_the_300_bar_window`. After restart s05 shows `RV24h 80th pct` (BTC) / `70th pct` (ETH).
  - Side finding: `GET /{sid}/backtest` on a cache miss replays inline in the API process; the first verification call (19:16 UTC, API code pre-hotfix) took 2925s and overwrote the worker's post-fix s05 report at 20:05 UTC. API restarted a second time (MM gate `safeToRestart:true, running:0` at 20:23 UTC) so its in-process replay runs the hotfixed modules; s05 regenerated via `refresh=true`.
**Tests**: 103 passed (`backend/tests/strategy_engine`). **Deploy**: overlay to /var/www/terminal (backend + frontend dist), worker restarted 19:13 / 19:33 / 19:42 / 19:48 UTC, API restarted twice after the MM slots gate (`safeToRestart:true, running:0` — 19:14 and 20:25 UTC). Report: `STRATEGIES_NO_GATES_REPORT.md`.

## 2026-09-04 — owner decisions A–E after the fire audit (symmetric crowding, s06 band label, s01 liq feed hookup, lost-fire replay)
**Why**: owner's written decisions on the 2026-09-04 fire audit ("0 fires; two s05 ETH fires lost to the oracle_stale bug").
**Files**: `strategy_engine/features/funding_gauge.py`, `strategy_engine/strategies/{base,s02_funding_flow,s06_hull_fisher_ema}.py`, `strategy_engine/{conditions,evaluator}.py`, `strategy_engine/backtest/runner.py`, `services/hyperliquid/tracker.py`, `docs/strategies/02-funding-settlement-flow.md` (§4), tests `test_features_databacked.py` (+1), `test_liquidation_ingest.py` (new).
**What changed**
- **A (owner-approved RULE CHANGE)**: crowding is symmetric — `abs(funding_z) >= 2` (either side, or the Binance thresholds) = EXTREME, `abs >= 1` = ELEVATED; `blocked_direction` from the sign; s02 already trades against the sign. Doc 02 §4 rewritten + rule-change note; `strat_changelog` row id 1 (`wallet='owner'`). Evidence: BTC HL funding z −3.75 at 09-03 21:00 UTC read as NORMAL under the one-sided rule.
- **B**: s06 0.5–3.0 ATR band left verbatim; new `band_occupancy()` → label `ema_band: inside 0.5–3 ATR of EMA200 on NN% of the last N 15m bars` on every s06/s06u signal (rendered as a tag on the detail page). New `StrategyContext.ohlc_15m_30d` (h,l,c over the 3000-bar window) in `_ctx` and the replay runner.
- **D**: s01 liquidation feed wired — the copy-tracker ws tier (≤10 userFills subs, API process) ingests fills carrying HL's `liquidation{markPx,method,liquidatedUser}` object into `strat_liquidations` for STRATEGY_ASSETS (BTC, ETH), `coverage='partial'`; side = the LIQUIDATED party's side (counterparty fill flips the maker's side); dedupe on ts/coin/liquidated_user/px/sz; snapshot fills ingested too (real timestamped history). Fixture = real prod fill (leader_trade_events id 4123055).
- **C/E — replay of the two "lost" s05 ETH fires (09-03 15:45 / 16:45 UTC) through the CURRENT code over the real tape (`/root/replay_s05.py`, dry run): NEITHER FIRES — `Volume` fails (0.40x / 0.21x of the 20-candle 15m taker avg). The stored score-1.0 evaluations came from the pre-`94d7556` s05, whose OR spanned ALL 300 candles (~3 days → "OR 5.80 ATR"; today-only OR = 1.42 ATR) and whose volume test compared the last 1-MINUTE taker row against a 20-row 1m average ("1.36x" — not the doc's break-candle rule). Nothing backfilled; the paper record stays honest. Hypothetical path (had every condition passed): long entry 2436.41 (OR high + 0.1 ATR), stop 2426.75, t1 2453.51, 1.2313 ETH ($3,000 notional, 3x CAPPED, $15 risk) → `entry_unfilled` (mid 2493.75 at signal, never returned to the OR edge within the 30-min TTL).
**Tests**: 108 passed locally. **Deploy**: overlay 05:2x UTC; worker restarted 05:32 UTC (stopped cleanly); MM gate `safeToRestart:true, running:0` at 05:32 UTC → API restarted 05:32 UTC (tracker ws reconnected, 9 subs, baselines seeded; no tracebacks post-restart). Gauge rows post-restart: BTC z +0.67 NORMAL, ETH z +0.22 NORMAL (no side currently ≥ |1|).

## 2026-09-04 — s05c "Session open — chase entry" comparison row (OWNER-APPROVED EXPERIMENT) + strat_liquidations backfill + s01 re-run
**Why**: owner GO (2026-09-04): "Add a comparison row s05c … identical rules to s05 except entry = post-only limit at the break candle close ± 0.1 ATR (re-quote per the paper executor's rules; taker fee if it must cross), stop/targets/time-stop unchanged. Runs in paper beside s05 exactly like 06/06u … The detail pages show fill rate (fills / fires)." Doc 05 stays verbatim; s05 itself is untouched.
**Files**: `strategy_engine/strategies/s05_session_open.py` (`SessionOpenMomentum(chase=True)` → id `s05c_session_open`), `strategy_engine/execution/manager.py`, `strategy_engine/{evaluator,seed,mode,conditions,contract}.py`, `strategy_engine/backtest/runner.py`, `routers/strategies.py`, tests `test_s05c_chase.py` (new, 8), `test_no_gates.py`/`test_conditions.py`/`test_safety.py` (registry/marker updates); frontend `types/strategies.ts`, `lib/strategiesApi.ts`, `design-b/screens/{StrategyDetailB,StrategiesOverviewB}.tsx`.
**What changed**
- **s05c** = the SAME `SessionOpenMomentum` class with `chase=True`: every condition, score, threshold and parameter identical (parameters copied from s05; conditions catalog identical; `REQUIRED` identical). Only the entry differs: `edge = break candle close` instead of the OR edge; `entry = close ± entry_offset_atr·ATR`; stop (`OR mid` if within 0.8 ATR else entry ∓ 0.8 ATR) / t1 (`entry ± OR height`) / 3h time-stop / 30-min entry TTL use the unchanged formulas from that entry. Intent meta carries `chase: True`; alerts tagged `[S05C SESSION]`.
- **Paper chase semantics (manager)**: placed post-only like s05 (would-cross → re-quote 1 tick inside the mid, max 3 → skip). Then, unlike s05 (which rests at its price until filled or TTL), a chase order FOLLOWS the market: each `manage()` cycle (30s) it is still unfilled it is re-quoted 1 tick inside the latest mid (the executor's re-quote rule, counted in the row marker `:chase=1:rq=N:rq_ts=T`); after the 3 re-quotes are spent it crosses as a **taker at the latest mid** (`:entry=taker` marker; `compute_close(entry_taker=True)` charges TAKER_FEE on the entry leg; closed rows keep `exit_reason='<reason>:entry=taker'` so the fee kind stays visible). A maker fill still happens whenever the tape prints through the resting price. TTL cancel (`entry_unfilled`) still applies. Stop/target prices are NOT moved by re-quotes.
- **Backtest model** (15m bars): a chase pending order fills maker if the next bar trades through it, otherwise taker at that bar's open (`entries_taker` counted in the summary line) — labelled as a resolution simplification.
- **Fill rate** on the API (`StrategyState.fires_total / fills_total / fills_taker / entries_pending / entries_unfilled / fill_rate`, all-time, counted from strat_signals/strat_trades) and a "Fill rate" cell on EVERY strategy detail page (`fills / fires · N taker · N resting · N unfilled`). Route id `05c`, tag "chase-entry comparison", EVIDENCE_ORDER puts 05c beside 05, `shortName` regex accepts the letter suffix.
- **strat_liquidations backfill** (`/root/backfill_liq.py`, owner GO): `leader_trade_events.raw` (`{"fill": <userFills fill>}` with the venue's `liquidation` object, source `hl_ws`, symbol BTC/ETH) mapped through the live path `tracker.liquidation_row`, dedupe = the live existence key (ts, coin, liquidated_user, px, sz), `coverage='partial'`. Result: **7,233 rows inserted (BTC 5,122 / ETH 2,111), 0 dupes, range 2026-08-11 14:52:07 → 2026-09-04 02:19:50 UTC**. s01 backtest re-run over that range offline via `runner.run_report` (`/root/bt_s01.py`) — results in `backend/backtest/results/s01_liq_sweep.md|.json`: **2026-08-11 14:52 → 09-04 06:29 UTC, 4,540 evaluations, 0 fires, 0 trades** (127s). Why 0: `Cascade size` met 3/233 (doc thresholds BTC $3M / ETH $1.5M per cascade vs partial-coverage rows averaging $20.5k BTC / $14.3k ETH), `Distinct wallets` 21/233 (min 25), `Timing` 0/133, and `Book refill`/`Taker exhaustion`/`CEX confirm`/`No next cluster` are "unavailable" before the 09-03 14:17 UTC book/trades coverage start (only 2.8% of the range). No condition was relaxed — the liquidation feed is a subset (copy-tracker wallets only, `coverage=partial`), which the doc's absolute-notional thresholds are not sized for; flagged for the owner, nothing changed.
- **First LIVE liquidation rows** (tracker ws ingest, API restarted 05:32 UTC): ids 7234/7235, 2026-09-04 06:36:34 UTC, BTC long, 0x6f8a8ca4…, px 80541 / 80538, sz 0.80712 / 0.80715, $65,006 each, method `market`, `coverage=partial` (two distinct fills of one liquidation, not a dupe).
**Tests**: 119 passed (`backend/tests/strategy_engine`). Frontend `tsc` unchanged (29 pre-existing errors, none in strategies files). **Deploy**: backend overlay + local `vite build` dist overlay (`VITE_COPY_LIVE_ENABLED=true VITE_COPY_MODE=live_manual`), nginx reloaded; worker restarted 06:38 UTC (stopped cleanly, seeded `s05c_session_open` state + 25 params); MM gate `safeToRestart:true, running:0` at 06:38 UTC → API restarted 06:39 UTC; `/api/strategies` returns 8 rows with the fill-rate fields, 0 tracebacks after restart.

## 2026-09-04 — HOTFIX: s05c taker-cross UPDATE failed on prod (exit_reason VARCHAR(64) too narrow) — migration v14
**Why**: first live s05c fire (ETH short, EU open 07:45:28 UTC). Chase re-quoted 2/3 at 07:46:03 and 3/3 at 07:46:36 UTC (2506.90 → 2505.80 → 2505.40); at 07:47:06 the executor tried to cross as taker @ 2504.25 and MySQL rejected the UPDATE: `1406 Data too long for column 'exit_reason'` — the marker `pending:hold_h=3:ttl_m=30:chase=1:rq=3:rq_ts=…:entry=taker` is 69 chars, column was 64. Retried and failed again at 07:47:37 (@ 2505.25); at 07:48:08 the tape printed through the resting 2505.40 and the row was recorded as a MAKER fill — a better price and a lower fee than the design intends, i.e. the record flattered the chase row. The local `_FakeSession` tests cannot see column widths.
**Files**: `app/db/strategy_models.py` (`StratTrade.exit_reason` String(128)), `app/main.py` (guarded migration v14: `ALTER TABLE strat_trades MODIFY exit_reason VARCHAR(128)` when width < 128), `tests/strategy_engine/test_s05c_chase.py` (+`test_marker_fits_exit_reason_column`: the longest marker/close reason must fit the model's column width).
**Prod**: column widened by hand 07:58 UTC (64 → 128, no restart needed — worker code unchanged; the migration is idempotent for the next API restart). **strat_trades row 2 corrected to what the executor logged it was writing**: fill_ts 1788508059617 → 1788508021951 (07:48:08 → 07:47:02 UTC), entry_px 2505.40 → 2504.25, marker + `:entry=taker`. Previous values kept here for revert. 120 tests.

## 2026-09-06 — Structure engine + heuristic Minds + models M1–M6 in paper mode (docs 10–17, one pass, owner pre-approved everything)
**Why**: owner (2026-09-06): "6 strategies model 11-M1…16-M6, heuristic mind 10-…, prompt 17-… implement everything in one go … start them for paper trading immediately … restart server after its done and recheck". Doc 17 rules: no phase gates, no AI calls, no live mode, 01–06 behaviour / RiskEngine limits / live path untouched, ambiguities recorded in `docs/models/DECISIONS.md`.
**Files (new)**: `app/strategy_engine/structure/` (13 modules), `app/strategy_engine/mind/` (base/snapshot/config/learn), `app/strategy_engine/model_runner.py`, `app/strategy_engine/strategies/{model_base,m1_sweep_reclaim,m2_bos_order_block,m3_failed_auction,m4_htf_choch,m5_session_liquidity_run,m6_weekly_open_reclaim}.py`, `app/db/migrations_models.py` (v15), `config/models.yaml`, tests `test_structure/test_mind/test_models_m1_m6/test_model_runner/model_fixtures`, `docs/models/{DECISIONS,IMPLEMENTATION-REPORT}.md`.
**Files (edited)**: `app/db/strategy_models.py`, `app/main.py` (v15 in lifespan), `routers/strategies.py` (`_NAMES` incl. models, Mind fields on signals/trades, `GET /{sid}/breakdown`, `GET/PUT /alerts/prefs`, model backtest → 404), `services/strat_outbox.py` (`[Mx]` kinds + per-wallet model filter), `strategy_engine/{conditions,contract,mode,scheduler,seed,worker}.py`, `execution/manager.py` (`AND model IS NULL`); frontend `types/strategies.ts`, `lib/strategiesApi.ts`, `design-b/screens/{StrategiesOverviewB,StrategyDetailB,SettingsB}.tsx`.
**What changed**: see `docs/models/IMPLEMENTATION-REPORT.md` §1. Summary — deterministic structure engine (swings/BOS/CHoCH/ranges/sessions/pools/sweeps/displacement/OB-FVG/VWAP/volume profile/day type/alignment table), Mind framework (weighted reasons, vetoes, multipliers → conviction → skip/half/full; in-trade checks; weekly learner gated 60 days), six models each with its own Mind on BTC+ETH, paper lifecycle (post-only fills on the book-mid tape, 40% partial at T1, BE, trail, T2/T3, thesis/dead-trade exits, r_multiple), one `strat_signals` row per model per coin per closed 15m with Mind fields even without a setup, migration v15 (4 new tables + columns), 66 model parameters seeded, Telegram `[Mx]` alerts with per-model Settings switches, Strategies page Model badge + MindPanel + Breakdown tab. 01–06 pages/behaviour unchanged.
**Tests**: 187 passed (67 new). Frontend tsc baseline unchanged (29 pre-existing, none in strategies files).
**Deploy (UTC)**: overlay ~22:35; worker restarted 22:42:19 (applied v15, seeded models); MM gate `safeToRestart:true, running:0` → API restarted 22:42:45 (up 22:51 — waited on a pre-existing unguarded `leader_trade_events` ALTER behind a metadata lock, not ours). Verification: 12 model signal rows at the 22:45 candle and again at 23:00; Mind fields on the API; `/strategies*` + `/settings` light+dark 0 console errors (Playwright); 4 `M1_pre` Telegram alerts delivered 22:51 to 3 linked chats. No fires yet (all tiers `none`, documented thresholds untouched).

## 2026-09-06 — AUDIT of M1–M6 against docs 10–16 (conformance fixes D-42…D-63, AUDIT-REPORT.md)
**Why**: owner (2026-09-06): "Audit the M1 to M6 implementation against docs 10 to 16 and write docs/models/AUDIT-REPORT.md. Fix only clear defects … Do not change any threshold, weight, multiplier or veto definition … Record every fix in DECISIONS.md … Do not touch strategies 01 to 06." Nine parts: spec conformance, mirror correctness, detector health on real data, snapshot/structure sanity, feed-missing veto, Mind math/wiring, paper realism, 30-day replay, report.
**Files**: `strategy_engine/strategies/m1…m6*.py`, `strategy_engine/mind/snapshot.py`, `strategy_engine/model_runner.py`, `strategy_engine/structure/{alignment,day_type}.py`; tests `test_models_m1_m6.py`, `test_model_runner.py`, NEW `test_models_mirror.py` (Part 2, 16 mirror cases) and `test_models_audit_wiring.py` (Parts 6–7 + detector-fault test); docs `docs/models/AUDIT-REPORT.md` (new), `DECISIONS.md` (D-42…D-63), docs 10–17 committed verbatim. 01–06 untouched.
**Fixes (defect classes only — no doc number changed)**: D-42 M1 undocumented `bias_gate` veto (blocked shorts when daily bias up) + `MAX_PER_DAY` cap REMOVED; D-43 M1 `second_close_below` cancel wired; D-44 M2 `delta_break`/`funding_young` mirrored for shorts, range multiplier 0.9, `htf_agree` tier, range-day precondition, `taken_zones` gate removed, OI ≥ −0.5 % step, entry validity; D-45 M3 close-below-prior-high step, `failure_quality` anchor, funding step, failures counter, dead-session levels; D-46 M4 `divergence` was INVERTED, `bounce_displacement` thresholds, T3 reachable, taker tape 7 d; D-47 M5 `early_in_window` anchor, `range_quality` constant 0 → body range; D-48 M6 reclaim window, `timing`, weekly freeze; D-50 M1 `delta_flip` mirrored (found by the mirror test); D-51 `feed_missing_book` veto removed (book optional per owner rule); D-52/53 M2 `late_day` symmetric, alt-alignment 8 h now-relative; D-54 M3 `second_failure`; D-56/57/58 M6 excursion depth, entry window anchor, prior-week multiplier reads frozen levels; D-59 M4/M6 `cohort_net_dir_entry` re-anchored at fill; D-60 M5 stop from deepest wick, `cleared` over the whole raid; D-62 `volume_2h_ratio` always None (window anchored on the last closed boundary — `no_trade` reachable); D-63 M5 `reclaim` detector raised → constant 0 (wired through the setup). D-55 examined/kept; D-61 `displacement_grade` constant 0.5 BY THE DOC'S FORMULA — reported (F-4), not changed.
**Findings not fixed (doc numbers / market / feeds)**: F-1 OI/taker feeds exist only since 2026-09-03 14:17 UTC → 27 of 30 replay days run without them (M1 loses 3.7 of 13.2 weight); F-5 liquidation `fuel` normaliser 0.10 %/0.08 % of OI = $2.2–2.8 M vs observed same-side bursts max $1.75 M BTC long / mean $139 k; F-6 4h trend "range" on 84 % of boundaries and NO 4h CHoCH in 30 days under doc 10 §2.2/2.3 → M2/M4 never align; F-7 M3 `cohort_adding_longs` 91 % (replay single cohort). Replay takes / 30 d: M1 3 (doc 20–60), M5 1 (15–40), M2 0, M3 0, M6 0 (0–8), M4 0 (0–6).
**Feed-missing veto (Part 5)**: 0 fires on prod since deploy; feed-era replay 8/520 per model = 1.5 %, all before the 14:17 feed start. Left as is.
**Tests**: 252 passed (`tests/strategy_engine`); full suite 252 passed + 5 PRE-EXISTING `tests/test_telegram_queue.py` async failures (unrelated, not touched). **Deploy**: overlay tar of the listed files + docs; worker restarted 2026-09-06 07:50:45 UTC (`seeded: 8 strategies + 6 models`); API NOT restarted (does not import model code — no MM-slots gate needed). 14 rows `requested_mode=paper effective_mode=paper`. Scratch artefacts on prod: `perpl_replay` DB, `/root/audit_tree`, `/root/audit_part8.py`, `/root/audit_stats.py` (documented in AUDIT-REPORT §9).

## 2026-09-06 — Spec v1.1: displacement/swing/trend corrections, liquidation history + calibrated normalisers, replay-only unavailable handling, 180-day replay (D-64…D-68)
**Why**: owner (2026-09-06): "Do the following in one continuous run without asking me anything … Part A spec v1.1 corrections … Part B liquidation history … Part C calibration … Part D replay data … Part E replay and report … Part F deploy." Strategies 01–06, Mind weights, vetoes and conviction thresholds untouched.
**Files (new)**: `strategy_engine/structure/calibration.py`, `strategy_engine/data/oxarchive.py`, `strategy_engine/data/binance_replay.py`, `strategy_engine/strategies/feed_inputs.py`, tests `test_calibration.py` (13), `test_unavailable.py` (5); docs `docs/models/REPLAY-6M.md`.
**Files (edited)**: `structure/{displacement,swings,trend,alignment,candles,pools}.py`, `strategies/{model_base,m1…m5}.py`, `mind/{base,snapshot}.py`, `model_runner.py`, `contract.py`, `routers/strategies.py`, `db/{strategy_models,migrations_models}.py` (v16), `main.py`, `worker.py`, tests `test_structure.py`/`test_models_audit_wiring.py`; frontend `types/strategies.ts`, `design-b/screens/StrategyDetailB.tsx`; docs 10–15, `DECISIONS.md` (D-64…D-68).
**What changed**
- **A1** `displacement_grade = clip(0.5·min(range/1.5ATR − 1, 1) + 0.5·min((body_ratio − 0.6)/0.3, 1), 0, 1)` (1.5 ATR/60 % → 0.0, 2.5 ATR/90 % → 1.0, 2.0 ATR/75 % → 0.5). **A2** swing strength k = 2 on 15m/1h/4h, 3 on daily; a trend needs the last 2 swing highs AND last 2 swing lows ascending (descending for down); M2 alignment alternative branch `trend_1h up + daily_bias up while trend_4h range` (mirror for shorts), logged in reasons_json.
- **B** `oxarchive.py` written against the documented API; **OXARCHIVE_API_KEY is absent locally and on prod → no history loaded, nothing fabricated** (D-65). Liquidation-map history (`strat_liq_map_hist`, source='fills') rebuilt from the live-feed fills with a 24 h forward horizon in 0.25 % bands at 15 m resolution: prod BTC 26,278 bands / 2,284 boundaries, ETH 16,541 / 2,097 (2026-08-11 → 09-06). Worker persists the live map each 15 m (source='live'). Live rows keep source NULL (= live); `month_counts('live')` counts NULL too. live_coverage = NULL (no archive rows).
- **C** `strat_calibration` (coin, key, value, computed_at, sample_count, window_days, note): liq_5m_p90_long/short, band_p80, live_coverage; daily 00:05 UTC + first run + weekly coverage. All seven fixed normalisers replaced (M1 fuel → liq_5m_p90 swept side, M1 eligible/uncleared → band_p80 / 50 %, M2 cluster_cleared → liq_5m_p90, M3 cluster_fuel → band_p80, M4 T2 → band_p80 and cluster_reward → 5×band_p80, M5 fuel → 0.8×liq_5m_p90, pools eligibility → band_p80). NULL calibration (window < 30 d) → previous 0.10 % OI fallback (D-66). Values logged on every recompute; Breakdown tab normaliser table (`NormaliserRow`; use-map keyed by model code — first deploy showed "not used by this model" on every row, fixed + regression test).
- **D** Binance USDT-M replay dataset on `perpl_replay` (`strat_replay_candles` 15m/1h/4h/1d 250 d, `strat_replay_oi` 5 m 30 d public limit; taker delta from the store where present else unavailable) (D-67). Replay-only `Snapshot.unavailable` → reasons excluded from both sums (`excluded_weight`), vetoes `unevaluated`, `effective_max_weight` (D-68). Live path never sets it.
- **E** 180-day replay BTC+ETH (`/root/replay_6m.py`, see REPLAY-6M.md §1 for the four replay patches incl. the adverse-first synthesized tape) + structure statistics (`/root/struct_stats.py`) → `docs/models/REPLAY-6M.md`.
**Tests**: 273 passed (`tests/strategy_engine`).
**Deploy (UTC, 2026-09-06)**: overlay tar (backend + docs + dist) 09:50, nginx reloaded; worker restarted 09:53 (v16 applied: `strat_liquidations += ['source']`, new tables via create_all), prod map rebuilt 09:54, worker restarted again 09:55 (systemd SIGKILL after the stop timeout — it was mid candle backfill; came up clean, `seeded: 8 strategies + 6 models`); MM gate `safeToRestart:true, running:0` → API restarted 09:57 and again 09:59 (router use-map fix), up 10:01. First-run calibration 09:59 boundary: BTC n=643/993/26,273, ETH n=275/473/16,537, all values NULL ("window 25.8d < 30d minimum") → OI fallback until ~2026-09-10. 14 rows `paper/paper`. M1–M6 evaluated BTC+ETH at 10:00 UTC; alignment: BTC trend_4h range (range 79,413..80,192, last 4h event CHoCH up), ETH trend_4h **up** (daily_bias up, last 4h event BOS up).

## 2026-09-06 — 0xArchive liquidation history loaded (owner supplied the key ~11:20 UTC): 30-day plan window, position-snapshot map, dedup + coverage semantics (D-69, D-70)
**Why**: owner: "use this 0xa_…" (the 0xArchive key) → Part B executed for real; D-65/D-66 amended. Key lives ONLY in prod `.env` (`OXARCHIVE_API_KEY`, pydantic Settings field added first — unknown .env keys crash the app), never printed, never committed.
**Files**: `app/config.py` (+`OXARCHIVE_API_KEY`), `strategy_engine/data/oxarchive.py` (plan-window 403 `history_window_exceeded` → `HistoryWindowExceeded` with the rolling "earliest allowed timestamp", `_paged` clamps start to it + 5 min margin, up to 3 re-clamps; `_any_ms` for the levels `snapshot_ts` string; dedup: live (ts,user,px) shadows archive fills, archive kept by multiplicity of (ts,user,px,sz); `_stream_coverage` cache; `map_rows` precedence levels > live > fills; `live_coverage` archive_basis stream/db), `model_runner.py` (`_refresh_archive` on first run + weekly before the coverage recompute), `mind/snapshot.py` (1/live_coverage scaling only in windows without archive rows), `structure/calibration.py` (`_band_notionals`: one map source per boundary by precedence), `tests/strategy_engine/test_calibration.py` (+4 → 276 passed), docs 10 §4.8/4.9, DECISIONS D-65/D-66 amended, D-69/D-70.
**Findings**: (1) the owner's key is FREE TIER — the API serves the most recent 30 days only; 180 days need the Build plan or a one-off Data Catalog purchase (owner's money decision, code needs nothing). (2) The archive stores maker-split fills as distinct rows (distinct `trade_id`, same `tx_hash`) sharing (ts,user,px,sz); a tuple dedup dropped 4,156 BTC / 1,487 ETH genuine fills on the first pass → multiplicity idempotence. (3) Our live feed = liquidation-flagged `userFills` of the copy-tracker wallets (`services/hyperliquid/tracker.py`, `coverage='partial'`) — hence live coverage of 6 % / 10 % by notional. (4) No live liquidation row since 2026-09-06 05:34 UTC (BTC) / 05:40 (ETH) — the tracker runs inside the API process, which is in the pre-existing OOM restart loop (`wallet_insights` fetchall of 4.46 M `leader_trade_events`); reported, fix still awaiting the owner's go.
**Prod load (`/root/oxarchive_load_prod.log`, 11:46–11:49 UTC, 301 requests)**: status `ok_plan_window`, effective start 2026-08-07 11:46 UTC (first fill 12:08). Archive rows per coin per month: BTC 2026-08 93,604 / 2026-09 16,963 (110,567; 7,942 shadowed by live rows); ETH 25,841 / 3,031 (28,872; 3,339 shadowed). Live rows (tracker): BTC 4,526 / 676, ETH 1,992 / 178. Map from 0xArchive projected-levels POSITION SNAPSHOTS (`source='levels'`): BTC 144,731 bands / 2,880 boundaries, ETH 141,845 / 2,880 (2026-08-07 12:00 → 09-06 11:45); fills proxy also written (BTC 43,430 / 2,799, ETH 44,611 / 2,674) as last fallback. Live coverage (3 d, archive stream basis): BTC 314 live rows $5.58 M vs 10,689 archive rows $93.25 M → **5.99 % notional / 2.94 % rows**; ETH 105 / $1.24 M vs 1,761 / $12.93 M → **9.61 % / 5.96 %**. The worker (restarted 11:39 UTC, before the bulk load) already did its own first-run coverage pull: 10,724 BTC / 1,629 ETH rows, coverage 0.0599 / 0.0961 — the weekly path works unattended.
**Calibration**: the loader's own recompute ran with the window at 29.98 d (first archive fill 08-07 12:08 vs now 11:46) → still NULL by minutes; recompute scheduled at 12:10 UTC with the same code path (`/root/recalib.py`, values recorded in D-69). The worker reloads the table every boundary, so it picks the values up at the next 15m boundary; daily 00:05 UTC recompute thereafter.
**Replay**: relaunched from scratch 11:52 UTC on `perpl_replay` with the archive-loaded tables (first run — no archive, old scaling — killed at 2026-06-13 = `/root/replay_6m_run2_noarchive_partial.log`); inside the replay calibration stays NULL (window < 30 d at every boundary), documented in REPLAY-6M §1.

## 2026-09-06 — 180-day replay finished + REPLAY-6M.md written, calibration populated on prod, Part F checks, commit (D-71…D-73)
**Why**: Part E/F of the owner's spec v1.1 run. No code change in this entry — report, decisions, evidence.
**Files**: `docs/models/REPLAY-6M.md` (new, 631 lines: method, funnel per model/coin, per-model performance, doc-17 frequency comparison, unavailable-handling weights, M1/M3 fuel section, structure statistics §7, findings, appendix with all 233 takes), `docs/models/DECISIONS.md` (D-71 M4 zero setups kept, D-72 M2 eight setup rows kept, D-73 how the replay may be used — no tuning from it).
**Replay (`/root/replay_6m.log`)**: 2026-03-10 09:30 → 2026-09-06 09:30 UTC, 17,281 boundaries × 2 coins, 16,776 s, 207,372 signal rows, 233 takes (189 filled + exited, 44 cancelled unfilled). Calibration NULL at every boundary (window < 30 d from the first liquidation row) → fallback normaliser throughout; liq/liqmap/OI available 16.7 % of the window, taker/gauge/book/cohort 1.5 %; day type `range` 84 % / `no_trade` 16 %.
**Per model (takes / 180 d, per 30 d, win rate, expectancy R, top exit)**: M1 139, 23.2, 34 %, −0.351 R (n=129), stop 68; M2 0; M3 55, 9.2, 37 %, −0.139 R (n=30), stop 18; M4 0; M5 18, 3.0, 33 %, −0.437 R (n=12), stop 7; M6 21, 3.5, 11 %, −0.369 R (n=18), thesis_failed 11. M4 zero traced to doc 14's extension precondition (max seen 3 consecutive 4h BOS / +5.31 % in 3 d vs ≥ 5 / ≥ 8 %); M2's 8 setup rows to "1h BOS that is a displacement candle" (4,148 / 3,450 rows failed it). Fuel question unanswerable (7 evaluated M1 takes, 0 M3).
**Prod state at 16:45 UTC**: `strat_calibration` BTC/ETH populated (band_p80 $21.42 M n=144,748 / $8.08 M n=141,855; liq_5m_p90 long/short $1.96 M n=1,801 / $2.92 M n=2,569 and $0.71 M n=1,059 / $0.60 M n=1,615; live_coverage 0.0599 n=10,689 / 0.0961 n=1,761; window 30 d); `strat_liquidations` 146,814 rows, span 29.9 d from 2026-08-07 12:08 UTC (139,442 archive + 7,372 live) — **30 days, not 180 (free-tier plan window, D-69)**; 14 rows paper/paper; worker 0 tracebacks since 14:10 UTC, 228 model evals (19 boundaries × 12). All changed backend files md5-identical local vs prod. Docs copied to `/var/www/terminal/docs/models/`.

## 2026-09-06/07 — Spec v1.2 (M4/M2 precondition corrections, point-in-time calibration, dual fill model)
**Files**: `backend/app/strategy_engine/strategies/m4_htf_choch.py`, `strategies/m2_bos_order_block.py`, `structure/displacement.py`, `structure/calibration.py`, `model_runner.py`, `backend/app/db/strategy_models.py`, tests (`test_structure.py`, `test_calibration.py`, `test_models_mirror.py`, `test_models_m1_m6.py`), docs 10/12/14, `DECISIONS.md` D-74…D-78.
**Why**: owner order "Spec v1.2 and a properly calibrated 6-month replay".
**What**:
- M4 extension: ≥3 consecutive 4h BOS or ≥5 % / 3 d; reason `clip((pct−3)/6)` (D-74).
- M2 break may be a ≤3-candle 1h displacement leg (combined range ≥1.5 ATR, net body ≥60 %), FVG inside the leg, OB = last opposite candle before the leg; `leg_at`/`detect_leg` added, global `detect` untouched (D-75).
- New table `strat_calibration_hist`; `asof_values`/`build_history`/`load_as_of`; replay-mode calibration lookups are as-of with NULL warnings; daily live recompute appends today's row (D-76).
- Replay driver gains `FILL_MODEL=adverse|neutral` (D-78); archive load requested for 215 d (D-77).
- Suite 280 passed. Strategies 01–06, weights, vetoes, thresholds untouched.

## 2026-09-07 — Spec v1.2 replay run (both fill models) + REPLAY-6M-v1.2.md + D-79…D-86, deployed and committed
**Why**: Parts 3–7 of the owner's v1.2 order. No engine code change in this entry beyond what the previous entry lists; this is the run, the report, the decisions and the prod confirmations.
**Files**: `docs/models/REPLAY-6M-v1.2.md` (new, 643 lines — §0 blocker, §1 method, §2 funnel + M4/M2 stage tables, §3 adverse vs neutral side by side incl. conviction/day-type/tier splits, §4 frequency, §5 D-80 effective weights, §6 fuel, §7 structure stats + §7.8 D-75 leg stats, §8 M6 exits + thesis_failed counterfactual, §9 feed presence per model, §10 calibration history / NULL count / 0xArchive rows per coin per month / live_coverage, §11 findings, §12 all 189 takes + the 5 neutral differences), `docs/models/DECISIONS.md` (D-79 API OOM config gate, D-80 unavailable set, D-81 plan blocker, D-82 neutral-pass divergence mechanism, D-83 M3/M6 take drop = D-80 semantics, D-84 M6 in-trade checks n=8, D-85 M4/M2 under D-74/D-75, D-86 structure stats unchanged). Prod scripts (not in repo): `/root/replay_v12.py`, `/root/replay_stats_v12.py`, `/root/v12_setup.py`, `/root/oxarchive_load_v12.py`, `/root/leg_stats_v12.py`, `/root/confirm_p7.py`.
**Blocker (D-81)**: prod 0xArchive key `tier: free` → loader clamped to 29 of 215 days ("plan history starts 2026-08-07T19:37:25"); tape 2026-08-07 12:08 → 2026-09-06 (30.5 d, 16.7 % of window); calibration history 182 as-of days per coin, 182 NULL (window 29.49 d < 30 d min); **34,562 NULL calibration lookups per pass** — "zero NULL lookups" confirmed FALSE. Not activated / not purchased (owner's decision).
**Runs**: adverse `perpl_replay` 17,666 s, neutral `perpl_replay_n` 17,372 s, 2026-03-10 19:45 → 2026-09-06 19:45 UTC, 17,281 boundaries × 2 coins, 0 tracebacks, 207,372 signal rows each; 189 / 188 model trades.
**Per model (takes / per 30 d / win rate / expectancy R, filled+exited n)**: adverse — M1 139 / 23.2 / 34 % / −0.351 (129); M2 0; M3 14 / 2.3 / 11 % / −0.220 (9); M4 0; M5 18 / 3.0 / 33 % / −0.437 (12); M6 18 / 3.0 / 20 % / −0.030 (15). Neutral — M1 138 / 23.0 / 34 % / −0.376 (129); M2–M6 identical to adverse (neutral rule acted on 4 candles, all M1; the rest is the recent_form + evaluate-before-manage cascade, D-82).
**Prod confirmations 2026-09-07 01:15 UTC**: worker boundaries since the 19:34 restart 24/24 with 12 model evaluations each, 0 tracebacks/ERROR in worker and API logs, API RSS 388 MB (D-79 gate holding); 14/14 `strat_strategy_state` rows paper; live liquidation feed receiving (copy-tracker rows since restart BTC 1 / ETH 5, archive incremental BTC 279 / ETH 23); calibration (window 30.51 d, computed 00:16 UTC): band_p80 BTC $21,325,600 n=147,249 / ETH $8,127,910 n=144,191; liq_5m_p90 long/short BTC $1,944,590 n=1,816 / $2,968,530 n=2,598, ETH $713,317 n=1,069 / $570,390 n=1,640; live_coverage BTC 0.0787 (n 4,121) / ETH 0.1498 (n 962); `strat_calibration_hist` on prod: as-of 09-06 NULL, as-of 09-07 populated (first non-NULL day). 0xArchive rows per coin per month: BTC 2026-08 93,742 / 2026-09 17,245; ETH 25,996 / 3,071; months 03–07: 0.

## 2026-09-07 — REPLAY-6M-v1.2 verification pack (no code change)
**Why**: owner order "Produce a verification pack for REPLAY-6M-v1.2 so I can check the results by hand, without changing any code."
**Files (docs only)**: `docs/models/replay-v1.2-takes.csv` (400 rows = every fired model signal in both passes, 18 ordered columns; sha256 `65ab2009…a37863`), `docs/models/REPLAY-6M-v1.2-VERIFICATION.md` (CSV column provenance, fired-without-trade disclosure, 8 seeded M1 takes with 12 candles each, summary script + SQL, table counts, commit/server/re-run), `docs/models/tools/{verify_pack_v12.py,summary_from_csv.py,summary.sql}`. Server: `/root/verify_pack_v12.py` (read-only, run twice), scratch `/root/why5.py`, `/root/prev_n.py`, `/root/tree_md5.py`.
**Findings**: (1) `fired` = 200 per pass vs 189/188 trade rows → 11 adverse / 12 neutral fired signals with no trade, all the `_open` duplicate guard: 6 (+1 M3) had a same-model trade still open past the signal; 4 (adverse) / 5 (neutral) had the previous trade exit inside the 15 min before the signal, which is not yet written because `run()` evaluates before `_mgr.manage` (D-82). Listed per signal with the trade id per DB (neutral ids differ: #531/#545). (2) `strat_trades.stop_px` is the LAST stop (BE/trail); CSV `stop_px` = `lifecycle_json.initial_stop` (what R divides by). `exit_px` lives only in `lifecycle_json.exit.px`. (3) `strat_replay_candles.ts` = kline close time − 1 ms. (4) Summary recomputed from the CSV matches §3 exactly (M1 139/129/−0.351; M3 14/−0.220; M5 18/−0.437; M6 18/−0.030; neutral M1 138/−0.376). (5) `/root/audit_tree/backend/app/strategy_engine` = commit `f1dcb75` (74/74 files md5-identical after CRLF normalisation). Table counts in the window: 15m 17,280/coin, OI 8,763/coin from 08-07, oxarchive liq BTC 110,758 / ETH 29,056, live liq 5,202 / 2,171, liq map levels 146,141 / 143,214, calibration_hist 182/182 NULL per key.

## 2026-09-07 — Spec v1.3: realistic post-only fills, stop floor, sweep confirmation (code + docs; replay/deploy follow in the next entry)
**Why**: owner order "Spec v1.3: execution realism and sweep confirmation" (7 parts; decide without asking, record from D-87, do not touch 01–06 or any Mind weight/veto/threshold).
**Files**: `backend/app/strategy_engine/execution/paper.py` (`post_only_place`, `would_cross`, `requote_inside`), `model_runner.py` (`ModelTradeManager.paper_post_only_quote` used by `_open` + M6 reprice + M1 FVG paths; `stop_floor_applied`/`entry_requoted`/`entry_px_orig` on the INSERT; `reclaim_candles`/`confirmation_used` on the signal), `strategies/model_base.py` (`STOP_FLOOR_ATR=0.5`, `stop_floor_tf`, `apply_stop_floor` in `evaluate`, `reclaim_entry_px`), `strategies/m1_sweep_reclaim.py` + `m5_session_liquidity_run.py` (confirmed reclaim + new entry/validity; M1 `conf`→`confm` shadowing bug fixed), `m2/m4/m6` (`stop_floor_tf = "1h"`), `structure/sweeps.py` (`Confirmation`, `wick_candle_index`, `confirmed_reclaim`), `mind/base.py` (`conviction = min(1.0, raw × Π)`), `db/migrations_models.py` (`apply_v17`) wired in `strategy_engine/worker.py` + `app/main.py`, `db/strategy_models.py` (ORM columns), `routers/strategies.py` (breakdown `splits` + `execution`, `_split_rows`), `contract.py` (`OutcomeSplit`, `ExecutionStats`), `frontend/src/types/strategies.ts`, `frontend/src/design-b/screens/StrategyDetailB.tsx` (Breakdown tab "Execution and rule splits (spec v1.3)"), tests `backend/tests/strategy_engine/test_spec_v13.py` (new, 16) + `test_models_m1_m6.py` fixtures (`confirm=` param, 4 new reclaim-type tests), docs 10 (§4.10 new, D-89 sweep bullet, D-90 cap, D-91 columns, Breakdown), 11/15 (sequence, entry, stop, prompt), 12/13/14/16/17 (post-only + stop-floor lines), `DECISIONS.md` D-87…D-92.
**What**: (1) post-only buy at/above the touch or sell at/below it → rejected, re-quoted once one tick inside, rests, fills only through `limit_fills_through`, cancelled at validity; logged in `lifecycle_json.post_only` + columns; same code live + replay (D-87). (2) 0.5 ATR(15m|1h) minimum stop distance applied centrally after `build_intent`; size and R from the final stop; targets untouched (D-88). (3) M1/M5 entry only after a confirmed reclaim (later-candle, or same-candle + one confirming 15m candle); entry `min(mid, close) − 0.05 ATR` (mirror), validity 3 candles from confirmation; `reclaim_candles` / `confirmation_used` on the signal (D-89). (4) conviction capped at 1.0 (D-90). (5) replay stats v13 + Breakdown tab splits/execution stats. Suite: 300 passed (`tests/strategy_engine`); `tsc` unchanged (28 pre-existing errors, none in touched files).

## 2026-09-07 — Spec v1.3 replay (both fill models), REPLAY-6M-v1.3.md, verification pack v1.3, D-93…D-98, deploy + prod confirmations
**Why**: Parts 6–7 of the owner's v1.3 order. No engine change beyond the previous entry; this is the run, the report, the pack, the decisions and the prod confirmations.
**Files**: `docs/models/REPLAY-6M-v1.3.md` (new, 692 lines — §0 rules-only re-measurement, §1 method + definitions, §2 funnel + D-89 population + cap, §3 adverse vs neutral, §4 the Part-5 splits (stop floor, re-quotes, reclaim type, re-price rejections), §5 v1.2 vs v1.3 per model, §6–§12 frequency / unavailable / fuel / structure / M6 / feeds / calibration, §13 findings, §14 all takes), `docs/models/replay-v1.3-takes.csv` (264 rows, 24 columns, sha256 `e24a587b…67ca`), `docs/models/tools/verify_pack_v13.py` (seed 42), `docs/models/DECISIONS.md` (D-92 log-name correction; D-93 M1/M5 population under D-89, D-94 M3 all-floored with fixed targets kept + flagged, D-95 re-quote / re-price measurements, D-96 adverse–neutral divergence = D-82, D-97 pre-existing M1 FVG second-attempt defect reported + fix proposed NOT applied, D-98 cap engaged / no take change), `docs/models/REPLAY-6M-v1.3-VERIFICATION.md` (second commit). Prod scripts (not in repo): `/root/replay_v13.py`, `/root/replay_stats_v13.py`, `/root/verify_pack_v13.py`; logs `/root/replay_v13_{adverse,neutral}.out`.
**Runs**: same data as v1.2 (`v12_setup.py` not re-run, D-92): adverse 14,426 s, neutral 14,385 s, 17,281 boundaries × 2 coins, 0 tracebacks, 207,372 signal rows each; 125 / 135 model trades. Engine tree 74/74 files md5-identical local / `/root/audit_tree` / `/var/www/terminal`.
**Per model v1.2 → v1.3 (takes / win rate / expectancy R)**: M1 adverse 139 / 34.1 % / −0.351 → 81 / 39.2 % / −0.310 (neutral 138 / 34.1 % / −0.376 → 91 / 41.8 % / −0.260); M2 0 → 0; M3 14 / 11.1 % / −0.220 → 14 / 11.1 % / −0.485; M4 0 → 0; M5 18 / 33.3 % / −0.437 → 12 / 50.0 % / −0.302; M6 18 / 20.0 % / −0.030 → 18 / 25.0 % / +0.001.
**Splits**: stop floor applied M1 12/81, M3 14/14, M5 1/12, M6 1/18; post-only rejections M1 10 per pass (12.3 % / 11.0 %), all filled better than the original limit (mean +11.34 bps favourable, +4.74 bps raw); M6 re-price rejections 3; M1 same-candle+confirmation 64 / later-candle 17 takes.
**Defect found (D-97, not changed)**: M1 FVG second attempt re-prices the entry without re-deriving stop / T1 → fills past T1 (#602, #603, #663, #683 −1.947 R). Fix proposed, awaiting go.
**Deploy**: overlay to `/var/www/terminal` + `/root/audit_tree`, MM slots gate checked (`running: 0`, `safeToRestart: true`) before `systemctl restart perpl-strategy-worker` + `perpl-terminal`; 14/14 `strat_strategy_state` rows paper; M1–M6 evaluated at the next two 15m boundaries, 0 tracebacks; migration v17 columns present on prod.

## 2026-09-22 — Audit findings against the strategy engine (no code changed)

**Files referenced (not modified)**
- `backend/app/routers/strategies.py:86,104,128,167,216`
- `backend/app/strategy_engine/scheduler.py:68,166`

**Findings**
- `/api/strategies` issues **642 queries / 9.59 s** per request. `:167` awaits inside a list
  comprehension (sequential over 14 rows); `_build_state` fires 3 more queries per row;
  `_latest_signals:104` loops per configured asset with one query each.
- Index/predicate mismatches inside that loop: `_trade_stats:86` filters `exit_ts` but the only
  index is `(strategy, fill_ts)`; `_latest_signals:104` filters `strategy AND asset` but the
  index is `(strategy, ts)` with no `asset`.
- Feed health (`:216` and `scheduler.py:68`) runs unqualified `COUNT(*)` over six feed tables —
  1.34 B rows examined on `strat_book_5s` alone — to populate a number the UI never shows.
- Liquidation `coverage` label is a hardcoded literal in the route handler; it will not track
  reality if coverage improves.

**Proposed (NOT applied — awaiting go)**
```sql
CREATE INDEX ix_strat_signals_strategy_asset_ts ON strat_signals (strategy, asset, ts);
CREATE INDEX ix_strat_trades_strategy_exit      ON strat_trades  (strategy, mode, exit_ts);
```
plus set-based `GROUP BY` replacing the per-row/per-asset loop, and `MAX(ts)` + approximate row
count replacing the feed-health `COUNT(*)`. Expected 642 queries → ~5; 9.59 s → <200 ms.
Use `lock_wait_timeout` + `ALGORITHM=INPLACE` per D-112.
